// Type declarations for @videojs/http-streaming
// The plugin auto-registers itself when imported and doesn't export types we need to reference
declare module '@videojs/http-streaming' {
  const httpStreaming: any;
  export default httpStreaming;
}
