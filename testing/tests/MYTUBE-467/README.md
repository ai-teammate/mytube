# MYTUBE-467 — Auth routes layout exclusion

Verifies that the `.shell` wrapper and associated CSS styles are absent on `/login` and `/register`.

**Framework**: Playwright (Python)  
**Scope**: UI / E2E  
**Routes under test**: `/login`, `/register`
